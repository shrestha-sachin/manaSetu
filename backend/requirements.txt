# FastAPI + ASGI server for Modal web endpoints
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0

# Modal (serverless)
modal>=0.64.0

# Google Gemini (official Gen AI SDK)
google-genai>=1.0.0

# HTTP client (Calendar API, Supabase REST, etc.)
httpx>=0.27.0

# Config / validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
supabase>=2.0.0
openai>=1.0.0
